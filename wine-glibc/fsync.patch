diff -urN proton10_wine/dlls/ntdll/unix/fsync.c b/dlls/ntdll/unix/fsync.c
--- proton10_wine/dlls/ntdll/unix/fsync.c	2025-10-26 19:03:17.862767796 +0800
+++ b/dlls/ntdll/unix/fsync.c	2025-10-26 20:44:28.338256368 +0800
@@ -216,7 +216,7 @@
 };
 C_ASSERT(sizeof(struct mutex) == 16);
 
-static char shm_name[29];
+static char shm_name[200];
 static int shm_fd;
 static volatile void *shm_addrs[8192];
 
@@ -570,11 +570,11 @@
         ERR("Cannot stat %s\n", config_dir);
 
     if (st.st_ino != (unsigned long)st.st_ino)
-        sprintf( shm_name, "/wine-%lx%08lx-fsync", (unsigned long)((unsigned long long)st.st_ino >> 32), (unsigned long)st.st_ino );
+        sprintf( shm_name, "/data/data/com.winlator/files/rootfs/tmp/shm/wine-%lx%08lx-fsync", (unsigned long)((unsigned long long)st.st_ino >> 32), (unsigned long)st.st_ino );
     else
-        sprintf( shm_name, "/wine-%lx-fsync", (unsigned long)st.st_ino );
+        sprintf( shm_name, "/data/data/com.winlator/files/rootfs/tmp/shm/wine-%lx-fsync", (unsigned long)st.st_ino );
 
-    if ((shm_fd = shm_open( shm_name, O_RDWR, 0644 )) == -1)
+    if ((shm_fd = open( shm_name, O_RDWR, 0644 )) == -1)
     {
         /* probably the server isn't running with WINEFSYNC, tell the user and bail */
         if (errno == ENOENT)
diff -urN proton10_wine/server/fsync.c b/server/fsync.c
--- proton10_wine/server/fsync.c	2025-10-26 19:03:20.245763670 +0800
+++ b/server/fsync.c	2025-10-26 20:42:51.885423384 +0800
@@ -68,7 +68,7 @@
 #endif
 }
 
-static char shm_name[29];
+static char shm_name[200];
 static int shm_fd;
 static off_t shm_size;
 static void **shm_addrs;
@@ -85,7 +85,7 @@
 static void shm_cleanup(void)
 {
     close( shm_fd );
-    if (shm_unlink( shm_name ) == -1)
+    if (unlink( shm_name ) == -1)
         perror( "shm_unlink" );
 }
 
@@ -97,14 +97,14 @@
         fatal_error( "cannot stat config dir\n" );
 
     if (st.st_ino != (unsigned long)st.st_ino)
-        sprintf( shm_name, "/wine-%lx%08lx-fsync", (unsigned long)((unsigned long long)st.st_ino >> 32), (unsigned long)st.st_ino );
+        sprintf( shm_name, "/data/data/com.winlator/files/rootfs/tmp/shm/wine-%lx%08lx-fsync", (unsigned long)((unsigned long long)st.st_ino >> 32), (unsigned long)st.st_ino );
     else
-        sprintf( shm_name, "/wine-%lx-fsync", (unsigned long)st.st_ino );
+        sprintf( shm_name, "/data/data/com.winlator/files/rootfs/tmp/shm/wine-%lx-fsync", (unsigned long)st.st_ino );
 
-    if (!shm_unlink( shm_name ))
+    if (!unlink( shm_name ))
         fprintf( stderr, "fsync: warning: a previous shm file %s was not properly removed\n", shm_name );
 
-    shm_fd = shm_open( shm_name, O_RDWR | O_CREAT | O_EXCL, 0644 );
+    shm_fd = open( shm_name, O_RDWR | O_CREAT | O_EXCL, 0644 );
     if (shm_fd == -1)
         perror( "shm_open" );
 
